module.exports = () => {
  return new Promise((resolve, reject) => {
    // Migration script
    // Monk request or Monk.db request ...
    // Ex:  return Monk.get('client').update({}, { $unset: { quality: 1 } }, { multi: true });
  });
};
